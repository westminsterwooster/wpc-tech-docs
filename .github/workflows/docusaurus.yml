name: Build Docusaurus

on:
  push:
    branches: ["main", "master", "gh-pages"]
  pull_request:
    branches: ["main", "master", "gh-pages"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install Pandoc
        uses: pandoc/actions/setup@v1

      - name: Install TeX Live
        run: sudo apt-get update && sudo apt-get install -y texlive-xetex texlive-latex-recommended texlive-fonts-recommended

      - name: Install dependencies
        run: npm ci

      - name: Build PDF
        run: npm run docs:pdf

      - name: Build site
        run: npm run build

      - name: Upload PDF artifact
        uses: actions/upload-artifact@v4
        with:
          name: manual-2026.05a
          path: static/manual-2026.05a.pdf

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: build

  deploy:
    if: github.event_name == 'push'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
